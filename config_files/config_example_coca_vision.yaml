settings:
  experiment_id: ${modalities_env:experiment_id}
  referencing_keys:
    sample_key: input_ids
    target_key: target_ids
  training:
    callback_interval_in_samples: 10000
    global_num_training_samples: 1925398
    global_num_seen_samples: 0
    do_apply_activation_checkpointing: true
    gradient_acc_steps: 1
    local_train_micro_batch_size: 512
    sequence_length: 512
    gradient_clipping:
      mode: p2_norm
      threshold: 1.0
  cuda_env:
    local_rank: ${cuda_env:LOCAL_RANK}
    global_rank: ${cuda_env:RANK}
    world_size: ${cuda_env:WORLD_SIZE}
  paths:
    checkpointing_path: data/checkpoints

tokenizer:
  component_key: tokenizer
  variant_key: gpt2_tokenizer_fast
  config:
    tokenizer_file: data/tokenizer/tokenizer_gpt2.json

collate_fn:
  component_key: collate_fn
  variant_key: coca_collator
  config:
    sample_keys:
      - feats
      - feats_len
      - ${settings.referencing_keys.sample_key}
      - modality
    target_keys: []
    text_sample_key: ${settings.referencing_keys.sample_key}
    text_target_key: ${settings.referencing_keys.target_key}

train_dataset:
  component_key: dataset
  variant_key: arrow_dataset_vision
  config:
    vision_dataset_arrows: coco_captions_arrow/train
    bpe_to_ind: bpe_to_ind_test.pkl
    bpecodes: bpecodes_test
    img_size: 224
    block_size_text_decoder: 512

    

val_dataset:
  component_key: dataset
  variant_key: arrow_dataset_vision
  config:
    vision_dataset_arrows: coco_captions_arrow/val
    bpe_to_ind: bpe_to_ind_test.pkl
    bpecodes: bpecodes_test
    img_size: 224
    block_size_text_decoder: 512
    

# train_dataloader:
#   component_key: data_loader
#   variant_key: repeating_data_loader
#   config:
#     dataloader:
#       component_key: data_loader
#       variant_key: default
#       config:
#         num_workers: 2
#         pin_memory: true
#         shuffle: false
#         dataloader_tag: "train"
#         dataset:
#           instance_key: train_dataset
#           pass_type: BY_REFERENCE
#         batch_sampler:
#           component_key: batch_sampler
#           variant_key: default
#           config:
#             batch_size: ${settings.training.local_train_micro_batch_size}
#             drop_last: false
#             sampler:
#               component_key: sampler
#               variant_key: distributed_sampler
#               config:
#                 rank: ${settings.cuda_env.global_rank}
#                 num_replicas: ${settings.cuda_env.world_size}
#                 shuffle: true
#                 dataset:
#                   instance_key: train_dataset
#                   pass_type: BY_REFERENCE
#         collate_fn:
#           instance_key: collate_fn
#           pass_type: BY_REFERENCE

#     num_epochs: 1
  

train_dataloader:
  component_key: data_loader
  variant_key: default
  config:
    num_workers: 2
    pin_memory: true
    shuffle: false
    dataloader_tag: "train"
    dataset:
      instance_key: train_dataset
      pass_type: BY_REFERENCE
    batch_sampler:
      component_key: batch_sampler
      variant_key: default
      config:
        batch_size: ${settings.training.local_train_micro_batch_size}
        drop_last: false
        sampler:
          component_key: sampler
          variant_key: distributed_sampler
          config:
            rank: ${settings.cuda_env.global_rank}
            num_replicas: ${settings.cuda_env.world_size}
            shuffle: false
            dataset:
              instance_key: train_dataset
              pass_type: BY_REFERENCE
    collate_fn:
      instance_key: collate_fn
      pass_type: BY_REFERENCE

val_dataloader:
  component_key: data_loader
  variant_key: default
  config:
    num_workers: 2
    pin_memory: true
    shuffle: false
    dataloader_tag: "val"
    dataset:
      instance_key: val_dataset
      pass_type: BY_REFERENCE
    batch_sampler:
      component_key: batch_sampler
      variant_key: default
      config:
        batch_size: ${settings.training.local_train_micro_batch_size}
        drop_last: false
        sampler:
          component_key: sampler
          variant_key: distributed_sampler
          config:
            rank: ${settings.cuda_env.global_rank}
            num_replicas: ${settings.cuda_env.world_size}
            shuffle: false
            dataset:
              instance_key: val_dataset
              pass_type: BY_REFERENCE
    collate_fn:
      instance_key: collate_fn
      pass_type: BY_REFERENCE

eval_dataloaders:
  - instance_key: val_dataloader
    pass_type: BY_REFERENCE

checkpointing:
  component_key: checkpointing
  variant_key: default
  config:
    checkpointing_strategy:
      component_key: checkpointing_strategy
      variant_key: save_k_most_recent_checkpoints_strategy
      config:
        k: -1 # -1 to save all checkpoints
    checkpointing_execution:
      component_key: checkpointing_execution
      variant_key: fsdp_to_disc_checkpointing
      config:
        checkpoint_path: ${settings.paths.checkpointing_path}
        global_rank: ${settings.cuda_env.global_rank}
        experiment_id: ${settings.experiment_id}
        mixed_precision_settings: FP_16
        sharding_strategy: FULL_SHARD
        block_names: [TransformerBlock, VisionTransformerBlock]


captioning_loss:
  component_key: loss
  variant_key: clm_cross_entropy_loss
  config:
    target_key: ${settings.referencing_keys.target_key}
    prediction_key: logits

contrastive_loss:
  component_key: loss
  variant_key: nce_loss
  config:
    prediction_key1: ${model.config.modality_cls_prediction_key}
    prediction_key2: ${model.config.text_cls_prediction_key}
    tag: contrastive_loss

loss_fn:
  - instance_key: captioning_loss
    pass_type: BY_REFERENCE
  - instance_key: contrastive_loss
    pass_type: BY_REFERENCE

wrapped_model:
  component_key: model
  variant_key: fsdp_wrapped
  config:
    model:
      instance_key: model
      pass_type: BY_REFERENCE
    sync_module_states: true
    mixed_precision_settings: FP_16
    sharding_strategy: FULL_SHARD
    block_names: [TransformerBlock, VisionTransformerBlock]

model:
  component_key: model
  variant_key: coca
  config:
    prediction_key: logits
    modality_key: modality
    modality_embd_prediction_key: modality_embeddings
    text_embd_prediction_key: text_embeddings
    modality_cls_prediction_key: modality_cls
    text_cls_prediction_key: text_cls
    modality_encoder_config:
      sample_key: feats
      prediction_key: modality_embeddings
      img_size: 224 # 288 in the original coca
      n_classes: Null # Disable vision transformer head
      n_layer: 3
      attention_config:
        attention_engine_type: default_attention
      n_head: 12
      n_embd: 768
      dropout: 0.0
      patch_size: 16 # 18 in the original coca
      patch_stride: 16 # 18 in the original coca
      n_img_channels: 3
      add_cls_token: False
      bias: True
    text_decoder_config:
      sample_key: ${settings.referencing_keys.sample_key}
      prediction_key: ${model.config.prediction_key}
      block_size: 512
      vocab_size: 610
      n_layer_text: 3
      n_layer_multimodal_text: 1
      attention_config:
        attention_engine_type: default_attention
      n_head: 12
      ffn_hidden: 2048
      n_embd: 768
      dropout: 0.0
      bias: true
      activation: fused_swiglu
      epsilon: 1e-5
    n_pool_head: 8
    n_vision_queries: 256
    n_audio_queries: Null
    bias_attn_pool: False
    epsilon_attn_pool: 1e-5
    weight_init:
      mean: 0.0
      std: 0.02

scheduler:
  component_key: scheduler
  variant_key: onecycle_lr
  config:
    optimizer:
      instance_key: optimizer
      pass_type: BY_REFERENCE
    max_lr: 6e-4
    div_factor: 10
    final_div_factor: 1
    total_steps: 962699
    pct_start: 0.01
    anneal_strategy: cos

optimizer:
  component_key: optimizer
  variant_key: adam_w
  config:
    lr: 0.00001
    betas: [0.9, 0.95]
    eps: 1e-8
    weight_decay: 1e-1
    wrapped_model:
      instance_key: wrapped_model
      pass_type: BY_REFERENCE

batch_progress_subscriber:
  component_key: progress_subscriber
  variant_key: rich
  config:
    local_rank: ${settings.cuda_env.local_rank}
    world_size: ${settings.cuda_env.world_size}
    global_num_seen_samples: ${settings.training.global_num_seen_samples}
    train_dataloader:
      instance_key: train_dataloader
      pass_type: BY_REFERENCE
    eval_dataloaders:
      instance_key: eval_dataloaders
      pass_type: BY_REFERENCE

evaluation_subscriber:
  component_key: results_subscriber
  variant_key: wandb
  config:
    local_rank: ${settings.cuda_env.local_rank}
    project: modalities
    mode: OFFLINE
    experiment_id: ${settings.experiment_id}
    directory: "."
